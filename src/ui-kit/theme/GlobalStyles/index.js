import { createGlobalStyle } from 'styled-components';
import * as defaultPlaceholderAnimationStyle from 'react-placeholder/lib/reactPlaceholder.css';

const GlobalStyles = createGlobalStyle`
  ${defaultPlaceholderAnimationStyle}
`;

export default GlobalStyles;
